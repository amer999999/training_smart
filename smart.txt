scaffold:
/home/odoo/workspace/Odoo_17/venv_17/bin/python ./odoo-bin scaffold real_estate /home/odoo/workspace/Odoo_17/my_adds

Check status of files	git status
See history of commits	git log
Create and switch to new branch	git checkout -b branch-name
Switch back to existing branch	git checkout branch-name
Merge a branch into current	git merge branch-name
Undo last commit (keep changes)	git reset --soft HEAD~1
Undo last commit (delete changes)	git reset --hard HEAD~1



sudo apt update && sudo apt upgrade -y

sudo apt install git python3-pip python3-dev python3-venv libxml2-dev libxslt1-dev libldap2-dev libsasl2-dev \
    libtiff5-dev libjpeg8-dev libopenjp2-7-dev zlib1g-dev libfreetype6-dev liblcms2-dev libwebp-dev \
    libharfbuzz-dev libfribidi-dev libxcb1-dev libpq-dev postgresql -y

wget https://github.com/wkhtmltopdf/packaging/releases/download/0.12.6.1-2/wkhtmltox_0.12.6.1-2.jammy_amd64.deb
sudo apt install ./wkhtmltox_0.12.6.1-2.jammy_amd64.deb

sudo -u postgres createuser --createdb --username postgres --no-createrole --no-superuser --pwprompt odoo

mkdir -p /opt/odoo/odoo17
git clone https://github.com/odoo/odoo.git --depth 1 --branch 17.0 /opt/odoo/odoo17

python3 -m venv /opt/odoo/odoo17-venv
source /opt/odoo/odoo17-venv/bin/activate
pip install -r /opt/odoo/odoo17/requirements.txt

[options]
; Database settings
db_host = localhost
db_port = 5432
db_user = odoo
db_password = your_secure_password

; Addons path
addons_path = /opt/odoo/odoo17/addons,/opt/odoo/custom_addons

; Logs
logfile = /var/log/odoo/odoo-server.log

# .gitignore
__pycache__/
*.pyc
*.egg-info/
venv/
.venv/
*.log
.idea/
.vscode/
odoo.conf  # Never commit: contains your DB password
.DS_Store
